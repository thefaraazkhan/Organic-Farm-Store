<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Product</title>
</head>
<body>
    <h1><%= farm.name %></h1>
    <h2>Add a product</h2>
    <!-- <form action="/products" method="POST"> -->
    <form action="/farms/<%=farm._id%>/products" method="POST">    
    
        <label for="name">Product Name</label>
        <input type="text" name="name" placeholder="Product Name" id="name">
        <label for="price">Price</label>
        <input type="number" name="price" step="any" placeholder="Price" id="price">
        <label for="category">Category</label>
        <select name="category" id="category">
            <% for(let category of categories) { %>
                <option value="<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <button>Submit</button>
        <div>
            <a href="/farms/<%=farm._id%>">Back To Farm</a>
        </div>
    </form>
</body>
</html>